{
  "name": "codex-discord-channel",
  "version": "0.2.0",
  "description": "Claude-style Discord channel ownership for Codex sessions.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/zhshgmail/codex-discord-channel.git"
  },
  "homepage": "https://github.com/zhshgmail/codex-discord-channel",
  "keywords": [
    "discord",
    "codex",
    "channel",
    "mcp",
    "session"
  ],
  "main": "runtime/mcp-server.cjs",
  "bin": {
    "codex-discord-channel": "bin/codex-discord-channel",
    "codex-discord-session": "bin/codex-discord-session"
  },
  "scripts": {
    "build": "npm run build:runtime",
    "build:runtime": "node scripts/build-runtime.js",
    "build:check": "node scripts/build-runtime.js --check",
    "syntax": "node --check bin/codex-discord-channel && node --check bin/codex-discord-session && node --check src/*.js && node --check scripts/*.js && node --check tests/unit/*.test.js && node --check tests/integration/*.test.js && node --check runtime/*.cjs",
    "test": "node --test tests/unit/*.test.js",
    "test:integration": "node --test tests/integration/*.test.js",
    "smoke": "node scripts/smoke.js",
    "import:bridge": "node scripts/import-bridge-state.js",
    "check": "npm run syntax && npm test && npm run test:integration && npm run build:check && npm run smoke"
  },
  "dependencies": {
    "discord.js": "^14.19.3",
    "undici": "^7.10.0",
    "ws": "^8.21.0"
  },
  "engines": {
    "node": ">=22"
  },
  "overrides": {
    "discord.js": {
      "undici": "^6.27.0"
    },
    "@discordjs/rest": {
      "undici": "^6.27.0"
    }
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
